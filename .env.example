# --- Base de données ---
# Dev local (docker compose up -d)
DATABASE_URL="postgresql://badminton:badminton@localhost:5432/badminton_score?schema=public"
# Prod (Vercel) : URL de la base badmin-score-db, à définir dans les env vars Vercel.

# --- Better Auth ---
# Générer avec : openssl rand -base64 32
BETTER_AUTH_SECRET=""
# URL publique de l'app. En local : http://localhost:3000
BETTER_AUTH_URL="http://localhost:3000"

# --- Compte par défaut (prisma/seed.ts) ---
SEED_USER_EMAIL="admin@badminton-score.local"
SEED_USER_PASSWORD="changeme123"
SEED_USER_NAME="Admin"
